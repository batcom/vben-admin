# CLAUDE.md – vben-admin

## Agent skills

### Issue tracker

Issues are tracked on GitHub (`batcom/vben-admin`). See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the default five-role label vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context monorepo — frontend (Vue Vben Admin) and backend (NestJS) have separate domain contexts. See `docs/agents/domain.md`.
